{
  "name": "gestion-empleados-api",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "deploy:dev": "sls deploy --stage dev --region sa-east-1",
    "deploy:stg": "sls deploy --stage stg --region sa-east-1",
    "offline": "sls offline --stage dev --region sa-east-1 --lambdaPort 3303 --httpPort 3300",
    "remove:dev": "sls remove --stage dev --region sa-east-1",
    "remove:stg": "sls remove --stage stg --region sa-east-1"
  },
  "author": "Pablo Urbano",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^12.0.4"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "http-status-codes": "^2.2.0",
    "knex": "^2.5.1",
    "mysql": "^2.18.1"
  }
}
